FROM python:3.12-slim
WORKDIR /app
RUN pip install --no-cache-dir fastapi "uvicorn[standard]"
COPY rabbit.py rabbit_service.py ./
EXPOSE 8080
CMD ["sh", "-c", "uvicorn rabbit_service:app --host 0.0.0.0 --port ${PORT:-8080}"]
