meta {
  name: GET rabbit no key → 401
  type: http
  seq: 5
}

get {
  url: http://localhost:8000/rabbit/1
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}

tests {
  test("missing key returns 401", function() {
    expect(res.status).to.equal(401);
  });
}
